apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: {{ .WorkloadCluster }}-{{ .App }}
spec:
  imageRepositoryRef:
    name: {{ .WorkloadCluster }}-{{ .App }}
  policy:
    semver:
      range: '>=0.0.0'
