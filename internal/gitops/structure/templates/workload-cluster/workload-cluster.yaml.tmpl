apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: {{ .ManagementCluster }}-clusters-{{ .Name }}
  namespace: default
spec:
  interval: 1m
  path: "./management-clusters/{{ .ManagementCluster }}/organizations/{{ .Organization }}/workload-clusters/{{ .Name }}"
  postBuild:
    substitute:
      cluster_name: "{{ .Name }}"
      organization: "{{ .Organization }}"
      {{- if .ClusterRelease }}
      cluster_release: "{{ .ClusterRelease }}"
      {{- end }}
      {{- if .DefaultAppsRelease }}
      default_apps_release: "{{ .DefaultAppsRelease }}"
      {{- end }}
  prune: false
  serviceAccountName: automation
  sourceRef:
    kind: GitRepository
    name: {{ .RepositoryName }}
  timeout: 2m
