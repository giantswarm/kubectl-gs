apiVersion: infrastructure.cluster.x-k8s.io/v1alpha4
kind: AzureCluster
metadata:
  labels:
    cluster.x-k8s.io/cluster-name: {{ .Name }}
  name: {{ .Name }}
  namespace: {{ .Namespace }}
spec:
  location: ""
  networkSpec:
    vnet:
      name: {{ .Name }}-vnet
  resourceGroup: {{ .Name }}
